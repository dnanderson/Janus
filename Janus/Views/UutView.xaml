<UserControl x:Class="Janus.Views.UutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Janus.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Test Info -->
        <GroupBox Header="Test Information" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <StackPanel>
                <TextBlock Text="{Binding SerialNumber, StringFormat='Serial Number: {0}'}" />
                <TextBlock Text="{Binding OperatorName, StringFormat='Operator: {0}'}" />
                <TextBlock Text="{Binding TestDescription, StringFormat='Description: {0}'}" />
                <TextBlock Text="{Binding ElapsedTime, StringFormat='Elapsed Time: {0:hh\\:mm\\:ss}'}" />
            </StackPanel>
        </GroupBox>

        <!-- Measurements -->
        <GroupBox Header="Measurements" Grid.Row="1" Grid.Column="0">
            <StackPanel>
                <TextBlock Text="{Binding Voltage, StringFormat='Voltage: {0:F2} V'}" />
                <TextBlock Text="{Binding Temperature, StringFormat='Temperature: {0:F1} °C'}" />
                <TextBlock Text="{Binding Current, StringFormat='Current: {0:F3} A'}" />
            </StackPanel>
        </GroupBox>

        <!-- Stop Button -->
        <Button Content="Stop Test" Grid.Row="1" Grid.Column="1" VerticalAlignment="Top" HorizontalAlignment="Right" Command="{Binding StopTestCommand}" />

        <!-- Logs -->
        <GroupBox Header="Serial Log" Grid.Row="2" Grid.Column="0" Grid.RowSpan="2">
            <ListBox ItemsSource="{Binding SerialLog}" />
        </GroupBox>
        <GroupBox Header="General Log" Grid.Row="2" Grid.Column="1" Grid.RowSpan="2">
            <ListBox ItemsSource="{Binding GeneralLog}" />
        </GroupBox>
    </Grid>
</UserControl>
